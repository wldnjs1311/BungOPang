using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Match3Puzzle : MonoBehaviour
{
    /*
     * 구조 설계
     * 1. n개의 타일 랜덤 생성 (match되지 않게)
     * 2. 인접한 타일 이동
     * 3. match 됐을 경우 삭제 -> 재료 추가
     * 4. 빈 공간 채우기
     * 5. match 가능한 타일이 없을 경우 재배열
     * 6. 
     */
    private void Awake()
    {
        
    }
}
